<%= content_tag :div, id: dom_id(vendor), class: 'card mb-5' do %>
  <div class="card-body">
    <div class="d-flex align-items-center">
      <%= icon 'resize-vert c-move mr-5' %>
      <div class="pr-5">
        <%= image_tag vendor.image_url(:thumbnail), width: 100, class: 'c-move' if vendor.image %>
      </div>
      <div class="pr-5">
        <%= content_tag :span, vendor.name, class: 'text-bold' %>
        <p class="tile-subtitle text-gray mb-0">
          <%= vendor.description %>
        </p>
      </div>
      <div class="ml-auto">
        <%= link_to 'Edit', edit_vendor_path(vendor), class: 'btn btn-primary' %>
        <%= link_to icon('cross'), vendor_path(vendor), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-error' %>
      </div>
    </div>
  </div>
<% end %>
