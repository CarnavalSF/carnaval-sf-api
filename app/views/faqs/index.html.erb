<div class="card bg-gray mb-5">
  <div class="card-body">
    <%= link_to 'New FAQ', new_faq_path, class: 'btn btn-success' %>
  </div>
</div>

<div class="sortable" data-update-url="<%= sort_path(class_name: 'faq') %>">
  <% @faqs.each do |faq| %>
    <%= render faq %>
  <% end %>
</div>
